---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-role-base-centos7
      image: centos/systemd
      image_version: latest
      ansible_groups:
        - group1
      privileged: True
verifier:
  name: testinfra
